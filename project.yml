name: KeepOne
options:
  minimumXcodeGenVersion: 2.38.0
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: "5.10"
    PRODUCT_BUNDLE_IDENTIFIER: com.example.keepone
targets:
  KeepOne:
    type: application
    platform: iOS
    sources:
      - path: KeepOne
        excludes:
          - Resources/Info.plist
    resources:
      - KeepOne/Resources
    info:
      path: KeepOne/Resources/Info.plist
      properties:
        NSPhotoLibraryUsageDescription: "KeepOne needs photo access to analyze similar images in a month you choose and suggest one to keep."
        NSPhotoLibraryAddUsageDescription: "KeepOne needs photo library write access to delete only photos you explicitly select."
    settings:
      base:
        PRODUCT_NAME: KeepOne
        DEVELOPMENT_TEAM: ""
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
  KeepOneTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - KeepOneTests
    dependencies:
      - target: KeepOne
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
